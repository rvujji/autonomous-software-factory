{
    "name": "@engineering/platform",

    "version": "1.0.0",

    "private": true,

    "type": "module",

    "main": "dist/index.js",

    "types": "dist/index.d.ts",

    "scripts": {
        "build": "tsc -b",
        "test": "vitest run --passWithNoTests",
        "clean": "rm -rf dist *.tsbuildinfo"
    },

    "dependencies": {
        "@engineering/shared": "workspace:*",
        "@engineering/core": "workspace:*",
        "@engineering/engine-pack": "workspace:*"
    }

}